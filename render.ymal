services:
  - type: web
    name: meibomian-segmentation
    env: python
    pythonVersion: "3.10"
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt --no-cache-dir
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app